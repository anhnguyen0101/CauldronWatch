// Deprecated: this component contained mock/sample data.
// The app now connects to the backend and uses live data. Keep this placeholder
// to avoid accidental imports — remove when you are ready to delete the file.

import React from 'react'

export default function PotionNetwork(){
  return (
    <div className="card">
      <h3 className="panel-title mb-3">Potion Network (deprecated)</h3>
      <div className="p-4 text-sm text-neutral-400">This component has been deprecated — use <code>PotionNetworkSVG</code> or <code>PotionNetworkGraph</code> which render live data from the backend.</div>
    </div>
  )
}
